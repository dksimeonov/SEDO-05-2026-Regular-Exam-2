name: CI

on:
  push:
    branches:
      - develop
      - 'feature/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore Homies.sln

      - name: Build application
        run: dotnet build Homies.sln --configuration Release --no-restore -p:TreatWarningsAsErrors=false

      - name: Run tests
        run: dotnet test Homies.sln --configuration Release --no-build --verbosity normal -p:TreatWarningsAsErrors=false